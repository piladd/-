version: '3.8'
services:
  messenger-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=db;Database=messengerdb;Username=postgres;Password=postgres
      - ASPNETCORE_URLS=http://+:80
      - Jwt__Secret=${JWT_SECRET}
      - Jwt__Issuer=MessengerApi
      - Jwt__Audience=MessengerClients
      - Jwt__ExpiresHours=12
      - Security__MasterKey=${MASTER_KEY}
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: messenger-db
    restart: always
    environment:
      POSTGRES_DB: messengerdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - messenger-api

volumes:
  pgdata:
